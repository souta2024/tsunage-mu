<% user_comments.each do |user_comment| %>
  <div class="post-row user-post-row">
    <div class="user-name">
      <span class="text-title">ユーザー名: </span>
        <%= link_to user_path(user_comment.user.account_id), class: "content-link-black" do %>
          <span><strong><%= user_comment.user.name %></strong></span>
        <% end %>
    </div>
    <div class="user-name">
      <span class="text-title">アカウントID: </span>
      <span><%= user_comment.user.account_id %></span>
    </div>
    <div>
      <span class="text-title">投稿内容:</span><br>
      <%= link_to post_path(user_comment.id), class: "content-link-black post-text-link" do %>
        <%= safe_join(user_comment.body.split("\n").map { |line| h(line) }, tag(:br)) %>
      <% end %>
    </div>
    <div>
      <%#= render "public/post_favorites/btn", post: user_comment %>
      <h1>いいねボタン</h1>
    </div>
    <div>
      <span class="text-title">投稿日時:</span>
      <span class="history-datetime"><%= time_ago_in_words(user_comment.created_at) %>前</span>
    </div>
    <div>
      <% if user_comment.comment_histories.present? %>
        <span class="text-title">最終更新日時:</span>
        <%= link_to post_update_history_path(user_comment.id), class: "history-link content-link-black" do %>
          <span class="history-datetime"><strong><%= l user_comment.updated_at, format: :long %></strong></span>
        <% end %>
      <% end %>
    </div>
  </div>
<% end %>